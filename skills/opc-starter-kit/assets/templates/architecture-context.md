# 架构上下文

> 在动工前定好技术边界。由 opc-starter-kit MVP阶段·步骤1 引导生成。

---

## 架构原则（不可妥协）

1. **[原则1]**
2. **[原则2]**
3. **[原则3]**

> 示例：
> 1. 状态管理统一使用 Zustand——不用 Redux
> 2. API 设计遵循 REST——不用 GraphQL（MVP 不需要）
> 3. 所有数据变更必须有审计日志

---

## 技术边界

### 使用
| 类别 | 选择 | 理由 |
|------|------|------|
| 语言 | *[如 TypeScript]* | *[理由]* |
| 框架 | *[如 Next.js]* | *[理由]* |
| 数据库 | *[如 PostgreSQL]* | *[理由]* |
| 部署 | *[如 Vercel]* | *[理由]* |
| 认证 | *[如 NextAuth.js]* | *[理由]* |

### 不使用（明确避开）
| 技术 | 避开理由 |
|------|---------|
| *[如 微服务]* | *[一人公司不需要微服务架构]* |
| *[如 Kubernetes]* | *[运维成本太高]* |

---

## 接受的取舍

*为了速度，我们接受：*

1. **[取舍1]**
2. **[取舍2]**
3. **[取舍3]**

> 示例：
> 1. MVP 阶段不做 SSR——全部用 CSR，等有 SEO 需求再加
> 2. 不做国际化——先只支持中文
> 3. 不做移动端适配——先用桌面端验证

---

## 安全基线

- [ ] 认证使用成熟方案（不自行实现密码存储）
- [ ] API 响应检查：不暴露敏感字段
- [ ] 用户输入验证：所有入口有验证层
- [ ] 依赖检查：无已知严重 CVE

---

## 会话约定

*每次 AI 编程会话的第一件事：加载本文档。*

*每次会话结束后更新的部分：*
- *做出的架构决策*
- *偏离原计划的地方及原因*
- *新发现的约束*

---

> 此文件是项目记忆的核心。每次新开编程会话时必须先加载此文件。